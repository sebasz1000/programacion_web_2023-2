# Hints

Here should be authContext context and authContextProvider provider
authContextProvider should have onLogin and onLogout functions to toggle isLogged context state true/false (with Firebase, it should work with Firebase auth login/logout functions)

